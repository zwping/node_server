
# .vscode/preview.yml
autoOpen: true # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 3000 # 应用的端口
    run: node index # 应用的启动命令
    root: ./ # 应用的启动目录
    name: node_server # 应用名称
    description: Node Web Server。 # 应用描述
    autoOpen: true # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
